language: node_js

node_js:
  - "stable"

script:
#  - npm run lint
  - npm run coverage:run
  - npm run build

after_script:
  - npm run coverage:upload

deploy:
  provider: pages
  local-dir: dist
  keep-history: true
  skip-cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: master