#!/bin/bash -e
# amq lulas test — run the Vitest suite once. Extra args pass through to vitest,
# e.g. `amq lulas test --watch` or `amq lulas test mitosis`.

case "$1" in
  -h|--help|help) awk 'NR>1 && !/^#/{exit} NR>1{sub(/^# ?/, ""); print}' "$0"; exit 0;;
esac

exec bun run test "$@"
