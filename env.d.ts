/// <reference types="vite/client" />

// Provided at build time by Vite's `define` (browser) and by the test runner
// (node). The test harness only uses them to derive a display label.
declare const __dirname: string;
declare const __filename: string;
